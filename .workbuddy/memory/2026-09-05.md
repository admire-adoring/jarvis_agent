# 2026-09-05

## jarvis_agent 新增 SSE 流式对话接口（v0.2 · N15 完成）

- 用户自己写了半成品（Controller 的 `POST /chat/sse` + `ChatService.chatStream` 声明），但 Impl 是从 `chat()` 复制的：返回 `Flux<String>` 却 return `String`（编译错误）、用的是阻塞 `.call()`。
- 修复 `ChatServiceImpl.chatStream`：空消息 → `Flux.just("请输入内容")`；`.stream().content()` 出流；保留 `ChatMemory.CONVERSATION_ID` advisor（多轮记忆）；`doOnNext/doOnComplete/doOnError` 日志 + `onErrorResume` 错误兜底。
- Controller 补 `produces = MediaType.TEXT_EVENT_STREAM_VALUE` —— Spring MVC 下 Flux 不声明该 produces 会按 JSON 数组编码而非 SSE 分帧。
- API 已 javap 核实 2.0.1：`ChatClientRequestSpec.stream()` → `StreamResponseSpec.content()` 返回 `Flux<String>`。
- 验证：JDK 21 + `-o` 离线编译 BUILD SUCCESS；`AI_DASHSCOPE_API_KEY=sk-test-placeholder ./mvnw -o test` 1/1 通过。
- MVP 文档已勾选 v0.2 · N15。
